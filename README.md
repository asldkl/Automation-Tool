# 三角洲行动自动化工具

基于图像识别的《三角洲行动》游戏日常自动化工具，支持 QQ 登录 + WeGame 快捷登录、多账号管理、定时执行、电源管理等功能。

## 功能特性

- **游戏内自动化** — 自动完成科技中心、工具台、护甲站、医疗站的生产/制作/领取
- **QQ 自动登录 + WeGame 快捷登录** — 多账号自动切换，完整自动化链路
- **定时执行** — 单次/每日循环，支持多个时间点，可设置运行前提醒
- **模板上传** — 在设置中可上传自定义模板图片，替换内置模板；支持内置图片预览，方便对照截图
- **一键出售** — 主流程完成后自动打开仓库，遍历售卖物品执行上架，支持降价设置、出售数量设置（适配产出数量>1的物品）
- **系统托盘** — 静默运行，后台执行任务
- **电源管理** — 唤醒电脑、自动关机、定时开机
- **邮件通知** — 执行完成后自动发送运行报告到邮箱，包含账号统计、已选操作、运行模式、一键出售结果、冷却状态
- **账号冷却管理** — 防止账号频繁执行，支持自定义冷却时间（默认8小时），含冷却状态查看和重置
- **自动领取邮箱货币** — 进入游戏后自动领取邮箱中的货币奖励
- **开机自启动** — 登录 Windows 时自动运行，可选立即执行任务
- **联网时间校验** — 启动时验证有效期
- **单实例保护** — 重复启动自动激活已有窗口

## 系统要求

- Windows 10 / 11
- Python 3.8+

## 安装与运行

```bash
pip install opencv-python numpy pyautogui psutil pywin32 pystray pillow
python main.py
```

打包为 exe：
```bash
pip install pyinstaller
pyinstaller 三角洲自动工具.spec
```

## 使用说明

1. 添加 QQ 账号截图，配置 QQ/WeGame 路径
2. 勾选需要自动执行的操作
3. 点击「开始运行」手动执行，或在设置中启用定时执行
4. 在「图像识别设置」中点击「上传模板图片」可替换内置模板，点击「预览」可查看当前模板图片，点击「恢复默认」可还原
   - 上传后自动生效，用户自定义模板始终优先于内置模板
5. 在模板上传向导底部可管理售卖物品图片，同一物品可重复添加以匹配产出数量
6. 在「自动任务设置」中可启用「一键出售」并设置降价次数（0-5次）、出售数量（1-99）
7. 在模板上传向导底部点击「出售测试」可单独测试出售流程（需先回到游戏仓库界面）

### QQ 截图优化建议

- 添加 QQ 账号截图后，右键选择「裁剪截图」框选包含头像+名称+QQ号的区域
- 头像轮廓、名称文字、QQ数字三个特征组合可精确区分不同账号
- 可使用「测试截图识别」验证匹配度，同时查看灰度匹配和边缘匹配结果
- 程序会自动尝试多尺度复合匹配（0.7x-1.3x 缩放），适配不同分辨率

### 一键出售说明

1. 出售相关模板（仓库、出售、上架、降价、确认上架）已内置在 `picture/One_Click_Sell/` 目录，可在模板上传向导中点击「预览」查看
2. 在模板上传向导底部的「售卖物品」区域，点击「添加物品」上传要出售的物品截图；同一物品可重复添加以匹配产出数量（如产出3个弹药则添加3次）
3. 截图规范：在仓库界面截取物品图标，确保物品图标清晰、无遮挡
4. 在设置中启用「主流程完成后执行一键售卖」并设置降价次数、出售数量
5. 主流程（制造/生产）完成后会自动按 Esc 回到主界面，然后执行出售流程
6. 出售流程：打开仓库 → 逐个识别物品 → 出售 → 上架 → 降价（按设置次数）→ 确认上架
7. 也可在模板上传向导中点击「出售测试」按钮单独测试出售流程（测试前请先回到游戏仓库界面）
8. 出售物品匹配置信度可在设置中调节（0.40-0.80），默认 0.55，较低值更宽松、较高值更严格

### 运行流程

清理进程 → 逐个账号：启动 QQ 登录 → 关闭 QQ → 启动 WeGame 快捷登录 → 进入游戏执行操作 → 关闭游戏和 WeGame → 完成

## 配置说明

用户配置：`~/.delta_auto_settings.json`，账号列表：`~/.delta_auto_accounts.json`，自定义模板：`~/.delta_auto_templates/`，冷却数据：`~/.delta_auto_cooldown.json`

| 配置项 | 说明 |
|--------|------|
| `wegame_path` / `qq_path` | WeGame / QQ 启动路径 |
| `confidence` | 图像匹配置信度（默认 0.7） |
| `run_mode` | 运行模式：单次 / 每日循环 |
| `schedule_times` | 定时执行时间列表 |
| `selected_operations` | 选中的自动操作 |
| `silent_mode` | 静默运行（托盘） |
| `wake_enabled` | 运行前唤醒电脑 |
| `auto_shutdown_enabled` | 自动关机 |
| `run_on_startup` | 开机立即运行 |
| `email_enabled` / `smtp_code` | 邮件通知配置 |
| `qq_mouse_move_distance` | QQ 账号列表鼠标下移距离 |
| `scroll_amount` | 滚动幅度（50-150） |
| `game_launch_wait` | 游戏启动等待时间（秒） |
| `enable_sell_after_run` | 主流程后执行一键出售 |
| `sell_discount_times` | 降价次数（0-5） |
| `sell_confidence` | 出售物品匹配置信度（0.40-0.80） |
| `sell_quantity` | 每个物品出售次数（1-99，适配产出数量>1的物品） |
| `enable_cooldown` | 启用账号冷却（默认8小时） |
| `cooldown_delay_minutes` | 账号间隔时间（0-5分钟） |
| `cooldown_run_immediately` | 冷却完立即运行（与定时执行互斥） |
| `cooldown_email_enabled` | 冷却到期邮件提醒 |
| `enable_email_currency` | 启用自动领取邮箱货币 |

## 更新日志

### v1.0.0 (2026-05-25)

- **修复主界面空格键误触**：在主界面按下空格键会意外打开上次关闭的窗口
  - 全局拦截空格键事件（`bind_all`），主界面任何位置按下空格均不响应
- **修复冷却监听日志重复**：冷却监听每 30 秒检查一次，每次都重复打印相同的就绪账号日志
  - 改为仅在就绪账号列表变化时才打印日志，避免刷屏
- **修复手动重置冷却后不执行**：手动重置账号冷却后，程序检测到冷却到期但未执行任务
  - 冷却监听暂停时间从 2 分钟缩短至 30 秒，提高手动重置后的响应速度
  - 冷却监听循环中增加唤醒定时器更新，确保冷却到期时电脑能被唤醒
- **修复一键出售降价失效**：点击「降价」按钮后鼠标遮挡导致后续降价失败
  - 每次点击「降价」后自动将鼠标移至屏幕左上角，避免悬浮提示遮挡下次点击
- **修复冷却到期前无法唤醒**：程序检测到账号冷却到期的前 5 分钟无法将电脑从休眠状态唤醒
  - 冷却监听循环中自动设置唤醒定时器，基于最早冷却到期时间提前 5 分钟唤醒
  - 手动重置冷却后自动更新唤醒定时器
  - `_set_next_wake_timer` 同时支持定时执行和冷却完立即运行两种模式
- **优化模板上传向导**：为不同功能的模板图片添加分组标题和分隔线
  - 分组：产出项设置、WeGame 登录、游戏内导航、设施操作、制造操作、一键出售、邮箱货币、QQ 登录
  - 方便用户快速定位和上传对应功能的模板图片
- **新增冷却到期邮件提醒**：账号冷却结束时自动发送邮件通知
  - 在「邮件通知」设置中新增「冷却结束后发送邮件提醒」开关
  - 独立于主邮件通知，可单独启用/禁用
  - 邮件包含到期时间、到期账号数量和账号列表
- **新增项目结构说明文档**：生成 `项目结构说明.txt`
- **版本号重置为 v1.0.0**

### v1.9.0 (2026-05-22)

- **优化图像识别算法**：新增多尺度复合模板匹配，结合灰度匹配和边缘匹配
  - 边缘匹配（Canny）对头像轮廓、名称文字笔画、QQ数字形状等结构特征敏感，能更好地区分不同账号
  - 自动尝试 0.7x-1.3x 多个缩放比例，解决模板与屏幕分辨率不匹配问题
  - QQ 自动登录流程（`qq_quick_login`）改用复合匹配，提高识别成功率
  - QQ 窗口激活降级方案同步升级
- **新增 QQ 截图裁剪工具**：右键菜单新增「裁剪截图（聚焦特征区域）」
  - 可视化框选包含头像+名称+QQ号的区域，三个特征组合可精确区分不同账号
  - 支持实时预览裁剪区域尺寸
- **优化截图识别测试**：同时显示灰度匹配度、边缘匹配度和复合多尺度匹配度
  - 边缘匹配度反映头像轮廓/文字/数字特征的匹配情况
  - 显示最佳缩放比例，方便判断分辨率差异

### v1.8.3 (2026-05-22)

- **修复冷却窗口布局问题**：重构冷却查看窗口的布局管理
  - 将 Treeview 和 Scrollbar 放入独立的 `tree_frame` 容器
  - 按钮区域固定在窗口底部，不再与 Treeview 水平并列
  - 彻底解决按钮被挤出可视区域或重叠的问题
- **移除强制4分钟账号间隔**：移除原有的固定4分钟最小间隔限制
  - 之前无论账号是否冷却，相邻账号都强制等待至少 4 分钟
  - 现在由用户自行设置账号间隔时间
- **新增账号间隔时间设置**：相邻账号之间加入固定等待间隔
  - 设置范围 0-5 分钟（默认 1 分钟），在「其他设置」中配置
  - 账号执行完毕后等待设定时间再执行下一个账号
  - 间隔时间使各账号的冷却到期时间自然错开，避免同时到期
- **修复间隔等待无法中断**：账号间隔等待改为分段 sleep，支持随时 Stop
  - 之前使用阻塞式 `time.sleep`，点击停止后需等待最长 5 分钟
  - 现在每 5 秒检查一次停止信号，点击停止后最多 5 秒响应
- **修复中断账号状态丢失**：被 Stop 中断的账号现在正确记录到处理列表
  - 之前中断的账号不出现在 processed_accounts 中，邮件通知中看不到
  - 现在中断的账号标记为「中断」并正确显示在运行报告中
- **修复 Stop 后残留进程**：手动停止后自动清理 Delta/QQ/WeGame 进程
  - 之前停止后游戏和登录器进程可能仍在后台运行
  - 现在 `on_finish` 中自动清理残留进程
- **修复死变量**：移除 `_start_cooldown_watcher` 中未使用的 `cd_delay` 变量

### v1.8.2 (2026-05-22)

- **新增一键重置所有冷却**：冷却查看窗口新增「一键重置所有」按钮
  - 点击后可一次性重置所有账号的冷却记录
  - 操作前弹出确认框，显示当前冷却账号数量
  - 适用于批量管理场景，无需逐个重置
- **新增双击账号手动冷却**：主界面账号列表支持双击手动记录冷却
  - 双击账号列表中的某一行，即可为该账号手动记录冷却时间
  - 冷却时长与设置中的冷却时间一致（默认8小时）
  - 操作前弹出确认框，显示冷却时长信息
  - 若该账号已在冷却中，会提示是否重新记录
  - 冷却功能未启用时会提示用户先在设置中开启
- **修复冷却窗口按钮显示不全**：增大冷却查看窗口尺寸，确保所有按钮完整可见
  - 窗口高度从 400 增加到 480 像素
  - Treeview 显示行数从 12 调整为 10 行，为按钮区域留出足够空间
- **修复中断账号误记录冷却**：用户手动停止运行时，当前账号不再记录冷却
  - 之前用户点击「停止」后，正在运行的账号仍会被记录冷却
  - 现在中断的账号不记录冷却、不计入成功/失败统计，可立即重试
  - 中断的账号在已处理列表中标记为「中断」而非「失败」
- **更新使用说明**：主界面帮助文档新增账号管理和冷却功能说明

### v1.8.1 (2026-05-22)

- **优化冷却重置交互**：冷却查看窗口新增双击重置功能
  - 双击列表中任意一行可直接弹出确认框重置该账号冷却
  - 重置确认弹窗增加"重置后该账号将可以立即运行"提示
  - 按钮重置与双击重置共用统一的确认逻辑
- **修复失败账号误记录冷却**：运行失败的账号不再记录冷却时间
  - 之前无论成功失败都会记录冷却，导致失败账号被迫等待 8 小时
  - 现在只有成功完成运行的账号才会记录冷却，失败账号可立即重试

### v1.7.1 (2026-05-21)

- **新增冷却完立即运行**：冷却结束后自动执行任务，无需等待定时时间点
  - 在「自动任务设置」中新增「冷却完立即运行」选项
  - 与「启用定时执行」互斥：勾选一个时另一个自动取消并禁用
  - 与「启用账号冷却」联动：勾选「冷却完立即运行」自动启用冷却，取消冷却自动取消此选项
  - 新增配置项 `cooldown_run_immediately`
- **优化账号切换间隔**：相邻两个账号开始执行的时间间隔强制不小于 4 分钟
  - 防止账号切换过快导致系统或游戏异常
  - 间隔不足时自动等待并显示倒计时
  - 仅对实际执行的账号计时，冷却跳过的账号不影响间隔计算
- **修复 QQ 窗口激活失败**：新增降级方案，解决 `SetForegroundWindow` 报错导致登录流程中断的问题
  - 等待 QQ 窗口时，若连续 5 次激活失败，自动启动降级方案
  - 降级方案：尝试图像识别点击 QQ 账号选择按钮（最多重试 3 次）
  - 若降级方案也失败，判定当前账号登录失败，跳过并发送失败通知邮件
- **增加烽火地带识别重试次数**：从 3 次增加到 5 次，提高低配置机器的识别成功率
- **优化游戏启动等待日志**：日志消息更清晰，显示"游戏已启动，额外等待 X 秒..."
- **修复冷却与定时任务冲突**：
  - 定时任务触发时临时忽略冷却检查，确保所有账号都能执行
  - 用户主动停止后，冷却监听等待 2 分钟再恢复，防止立即重新触发
  - 程序启动时校验互斥，防止定时执行与冷却完立即运行同时启用
  - 冷却监听线程异常退出后自动重启（5 秒恢复机制）
  - 首次启用「冷却完立即运行」时，为所有账号记录冷却时间，防止立即全部执行

### v1.8.0 (2026-05-21)

- **新增账号冷却管理**：支持对每个账号设置冷却时间（默认8小时），防止频繁执行
  - 冷却数据持久化存储在 `~/.delta_auto_cooldown.json`
  - 主界面新增「查看冷却」按钮，弹窗展示所有账号的冷却状态（上次运行、剩余时间、下次运行）
  - 支持在冷却查看窗口中重置选中账号的冷却
  - 冷却延迟支持随机 0~N 分钟偏移，防止多账号同时到期
  - 在「其他设置」中可启用冷却功能并设置延迟时间上限（0-10分钟）
- **新增自动领取邮箱货币**：进入烽火地带后，自动识别并领取邮箱中的货币奖励
  - 依次点击：邮箱入口 → 交易中心 → 全部领取 → 领取完成
  - 未识别到邮箱入口时自动跳过，不影响后续操作
  - 在「其他设置」中可启用该功能
- **优化邮件通知**：
  - 单个账号失败时立即发送邮件通知（不等所有账号完成），邮件标题标注出错账号名
  - 已处理账号列表新增「下次运行时间」列，显示每个账号的冷却状态
- **邮件通知增强**：运行报告和失败通知中的账号列表均显示下次运行时间信息

### v1.7.1 (2026-05-20)

- **修复开机自启动失败**：程序通过注册表开机自启动时，由于系统网络尚未就绪，联网时间校验全部失败导致程序无法启动
  - 新增联网重试机制：开机自启动模式下自动重试最多 6 次（每次间隔 5 秒），等待网络就绪
  - 加载界面实时显示重试进度（"网络未就绪，正在重试 (2/6)..."）
  - 重试耗尽后自动降级：有本地记录则使用记录继续，无记录则使用系统日期兜底（仍校验有效期）
  - 非自启动模式（手动双击）保持原有行为不变
- **修复打包遗漏**：补充 `One_Click_Sell` 目录到打包配置，确保一键出售模板正确打包

### v1.7.0 (2026-05-19)

- **售卖物品支持重复添加**：同一物品图片可多次添加，适配产出数量大于1的物品（如弹药、医疗包等），无需手动复制图片
- **出售测试按钮移至模板上传向导**：从主界面移至模板上传向导页面的售卖物品区域，旁边附带使用说明
- **优化设置布局**：出售数量设置移至一键出售区域独立行，避免被遮挡

### v1.6.0 (2026-05-19)

- **一键出售增加出售数量设置**：支持设置每个物品的出售次数（1-99），适配产出数量大于1的物品（如批量生产的弹药、医疗包等）
  - 在「自动任务设置」中新增「出售数量」选项
  - 出售流程会对每个物品重复执行指定次数
  - 邮件通知中的出售统计已适配新的数量逻辑

### v1.5.0 (2026-05-19)

- **增强防破解保护**：
  - 多重时间源交叉验证（淘宝、苏宁、世界时间API）
  - 时间偏移检测，防止系统时间被回拨
  - 有效期加密存储，防止直接查看
  - 关键代码段混淆，增加反编译难度
- **优化启动流程**：所有时间源都失败时允许运行，避免影响正常使用

### v1.4.2 (2026-05-19)

- **增强邮件通知功能**：程序运行失败或异常退出时，自动发送失败通知邮件，包含错误信息和已处理的QQ号名称列表
- **邮件附带QQ号信息**：运行报告邮件中新增「已处理账号」列表，显示每个QQ号名称及其运行状态（成功/失败）
- **优化出售流程**：确认一键出售功能在未找到售卖物品时正确跳过并执行下一个物品（已有功能验证）

### v1.4.1 (2026-05-18)

- **修复图像识别点击失效**：修复 `utils.py` 中 `find_and_click` 函数的缩进/逻辑错误，导致匹配成功后无法执行点击操作。原因是点击代码（`moveTo` + `click`）被错误地放在了 `elif` 分支中（仅在置信度未达标时执行），而非 `if max_val >= threshold` 分支内。同时移除了该 `elif` 分支，避免在未达阈值时误点击。

## 技术栈

Tkinter / OpenCV / PyAutoGUI / psutil / pywin32 / pystray+Pillow / PyInstaller

## 注意事项

- 运行时请勿遮挡屏幕
- 建议 1920x1080 100% 缩放，识别失败可降低置信度
- 电源管理需管理员权限
- 有效期至 2026 年 7 月 1 日
- 程序目录路径支持中文等非 ASCII 字符
- 首次运行需要联网验证时间
- 如果系统时间被篡改，程序将拒绝启动

## 免责声明

本工具仅供个人学习研究使用，请勿用于违反游戏用户协议的行为。
