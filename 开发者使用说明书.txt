================================================================================
                    三角洲行动自动化工具 - 开发者使用说明书
================================================================================

版本: 6.08.22
日期: 2026-08-21
作者: 开发团队

================================================================================
目录
================================================================================

1.  项目概述
2.  文件结构
3.  模块拆分架构（v1.1.0）
4.  防滥用保护机制
5.  机器指纹机制详解
6.  服务器验证流程
7.  Interception 驱动级键盘输入
8.  心跳同步机制
9.  如何管理用户（白名单/有效期）
10. 打包说明（PyInstaller）
11. 打包说明（Nuitka）
12. 制作安装程序（Inno Setup）
13. 更新日志
14. 服务器部署
15. 常见问题

================================================================================
1. 项目概述
================================================================================

本工具是《三角洲行动》游戏的日常自动化工具，基于图像识别 + OCR 文字识别技术，支持：
- WeGame 直接登录（Interception 驱动级键盘输入）
- 多账号管理
- 冷却执行（冷却到期自动运行）
- 自动关机
- 邮件通知
- OCR 文字识别（RapidOCR）
- 机器指纹绑定（防止滥用）
- 远程有效期控制（服务器端校验）
- 账号状态实时同步（心跳推送）

================================================================================
2. 文件结构
================================================================================

项目根目录/
├── main.py                  # 主入口文件
├── gui_app.py               # 图形界面主程序（入口层，约 900 行）
├── automation_runner.py     # 自动化运行模块（v1.1.0 新增）
├── scheduler.py             # 定时调度模块（v1.1.0 新增）
├── account_manager.py       # 账号管理模块（v1.1.0 新增）
├── email_notifier.py        # 邮件通知模块（v1.1.0 新增）
├── cooldown_watcher.py      # 冷却监听模块（v1.1.0 新增）
├── server_client.py         # 服务器通信模块（v1.1.0 新增）
├── automation.py            # 自动化流程模块（游戏操作、设施处理、一键出售）
├── config.py                # 配置管理模块（含 resource_path 资源定位）
├── machine_fingerprint.py   # 机器指纹模块（防止滥用/授权校验）
├── utils.py                 # 工具函数模块（图像识别、OCR、进程管理等）
├── settings_window.py       # 设置窗口（支持 Canvas+Scrollbar 滚动）
├── template_capture.py      # 模板截图工具（含 OCR 配置）
├── cooldown_manager.py      # 冷却管理模块
├── relative_mouse_move.py   # 贝塞尔曲线鼠标移动模块（v1.1.2 新增）
├── asset_db.py              # 资产数据 SQLite 持久化模块（v1.1.1 新增）
├── skin_sniper.py           # 皮肤抢购模块（图片匹配查找按钮，自动购买确认）
├── interception_keyboard.py # Interception 驱动级键盘输入模块（v1.2.0 新增）
│                           # 支持 DLL 搜索路径：MEIPASS→exe目录→LCA安装目录→System32
│                           # 自动检测键盘设备号（跳过只读 device 1，使用 device 2+）
│                           # filter=0 不拦截事件，只用于发送
├── driver_keyboard.py       # 键盘驱动统一入口（委托 interception_keyboard）
├── custom_ops.py            # 自定义操作模块（v1.3.6 新增）：步骤持久化 + 主流程后自动执行「找图→单击」
├── custom_ops_window.py     # 自定义操作配置向导（v1.3.6 新增）：屏幕框选/导入图片，步骤编辑/排序/测试运行
├── test_all_features.py     # 全功能测试套件（43 项）
├── test_core_modules.py     # 核心模块单元测试（v1.1.4 新增，28 项）
├── README.md                # 用户说明文档
├── 开发者使用说明书.txt      # 本文件
├── 项目结构说明.txt          # 项目结构速查
├── 三角洲自动工具.spec       # PyInstaller 打包配置
├── installer.iss            # Inno Setup 安装程序脚本
├── server.py服务器主程序.txt # 服务器后端代码（部署到云端）
├── 图形化可视管理后台代码.txt # 管理后台前端代码
├── picture/                 # 图片资源目录
│   ├── icon/                # 程序图标
│   │   └── icon.ico
│   ├── Facility_Controls/   # 设施图标
│   ├── Crafting_Controls/   # 制造按钮
│   ├── Navigation/          # 导航图标
│   ├── One_Click_Sell/      # 一键出售模板
│   ├── email/               # 邮箱货币模板
│   ├── wegame_login/        # WeGame 登录模板（account_select、Input、Sign-in、delta_game_icon、delta_launch_btn、login_again）
│   └── produce/             # 生产设施模板
├── build/                   # PyInstaller 构建临时目录
├── dist/                    # PyInstaller 打包输出目录
├── dist_nuitka/             # Nuitka onefile 打包输出目录
├── dist_nuitka_standalone/  # Nuitka standalone 打包输出目录
│   └── main.dist/           # standalone 分发文件夹
│       ├── 三角洲自动工具.exe
│       ├── picture/         # 数据文件
│       └── *.dll / *.pyd    # 依赖文件
└── installer_output/        # Inno Setup 安装程序输出目录
    ├── 三角洲行动自动化工具_安装程序.exe
    └── 使用说明.txt

================================================================================
3. 模块拆分架构（v1.1.0）
================================================================================

v1.1.0 将 gui_app.py（原约 2926 行）拆分为 7 个文件，采用模块级函数 + app 参数的模式：

----------------------------------------
3.1 设计模式
----------------------------------------

所有子模块使用模块级函数，接收 `app` 参数（App 实例）：

    # gui_app.py
    import automation_runner

    class App:
        def start(self):
            automation_runner.start_run(self)

    # automation_runner.py
    def start_run(app):
        # 直接访问 app.root, app.log_area 等
        ...

优点：无需继承、无循环依赖、函数可独立测试。

----------------------------------------
3.2 模块职责
----------------------------------------

gui_app.py（入口层，约 900 行）
    - App 类定义、__init__、_build_ui、_setup_styles、_setup_tray
    - 所有业务逻辑委托给子模块
    - 保留 UI 构建和窗口管理代码

automation_runner.py（约 596 行）
    - start_run / stop_run：启动/停止自动化任务
    - run_script_main：主自动化循环（开始前和结束后均检查 10 分钟内到期账号）
    - _login_account：WeGame 直接登录（Interception 驱动级键盘输入账号密码，图像识别点击登录按钮）
    - _launch_game：查找三角洲图标、启动游戏、进入特勤处、资产识别（两次：进入特勤处后+操作完成后）
    - _recognize_and_store_asset：资产识别并存储结果（支持 stage 参数区分识别阶段）
    - _get_cooldown_key：统一冷却 key 匹配（优先短名称，兼容 account: 前缀）
    - _wait_and_run_nearby_cooldowns：检查并等待 10 分钟内到期的账号
    - start_single_account_run / _run_single_account_main：单账号运行（进入特勤处+资产识别，不执行游戏内操作）
    - game_operations_wrapper / sell_operations_wrapper
    - on_finish：任务完成后处理（延迟关机等）

scheduler.py（约 100 行，v1.1.4 精简）
    - set_next_wake_timer：计算下一个冷却到期时间，设置唤醒定时器
    - update_cooldown_wake_timer：冷却监听专用唤醒定时器更新
    - apply_auto_settings：应用冷却执行设置

account_manager.py（约 738 行）
    - 账号 CRUD：save/load/add/delete/clear
    - 排序：move_up / move_down
    - Treeview 管理：refresh_account_tree（四列：账号名称/现有资产/下次运行时间/备注）
    - 冷却 UI：reset_selected_cooldown / custom_cooldown_time（双击「下次运行时间」列打开，两个数字框 + 固定 H/M 单位，如 8H 30M）/ reset_all_cooldowns
    - double_click_next_run：仅当双击 next_run 列时打开自定义冷却时间（替代原右键「自定义冷却时间」）
    - 账号信息设置：_open_account_info_window（游戏账号/密码/备注）
    - 资产监测：show_asset_monitor（自定义天数统计）

email_notifier.py（约 250 行）
    - send_run_report_email：运行完成报告
    - send_failure_email：运行失败通知
    - send_account_failure_email：单账号失败通知

cooldown_watcher.py（约 160 行）
    - start/stop/restart_cooldown_watcher
    - cooldown_watcher_loop：每 30 秒检查冷却到期
    - check_any_account_ready：检查是否有账号冷却到期（排除暂停账号，使用 _get_cooldown_key 统一 key）

server_client.py（约 117 行）
    - validate_with_server：启动验证
    - start/stop_heartbeat：心跳管理
    - heartbeat_loop / send_heartbeat
    - update_account_status

----------------------------------------
3.3 调用关系图
----------------------------------------

gui_app.py
  ├── automation_runner.py → automation.py
  ├── scheduler.py → cooldown_watcher.py
  ├── account_manager.py
  ├── email_notifier.py
  ├── server_client.py
  ├── cooldown_watcher.py
  ├── utils.py（图像识别 / OCR / 进程管理 / 电源管理）
  ├── config.py
  ├── settings_window.py
  ├── template_capture.py
  ├── cooldown_manager.py
  └── machine_fingerprint.py

================================================================================
4. 防滥用保护机制
================================================================================

v1.0.3 采用全新的服务器端验证架构，彻底移除了本地有效期校验：

旧机制（已移除）：
    - 本地 XOR 加密有效期
    - 多时间源交叉验证（淘宝/苏宁/世界时间API）
    - 本地时间戳防篡改（~/.delta_auto_timestamp.dat）
    - 以上代码已全部删除，不再使用

新机制（v1.0.3）：
    - 机器指纹绑定：通过硬件序列号生成唯一指纹，程序只能在授权电脑运行
    - 服务器端有效期校验：有效期由服务器统一管理，无法本地篡改
    - 双重验证：启动时验证 + 每天首次运行任务时验证
    - 心跳同步：运行时每30秒推送账号状态到服务器

================================================================================
5. 机器指纹机制详解
================================================================================

文件: machine_fingerprint.py

----------------------------------------
5.1 指纹生成算法
----------------------------------------

1. 通过 WMI 获取硬盘序列号：
   命令: wmic diskdrive get serialnumber

2. 通过 WMI 获取主板序列号：
   命令: wmic baseboard get serialnumber

3. 拼接两个序列号（格式: "DISK_SERIAL:BOARD_SERIAL"）

4. 对拼接字符串进行 SHA256 哈希

5. 取哈希值前32个十六进制字符作为机器指纹

降级方案：如果 WMI 调用失败，使用 hostname + MAC 地址作为备用标识

----------------------------------------
5.2 核心函数
----------------------------------------

    get_machine_id() -> str
        返回32位十六进制机器指纹字符串
        示例: "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6"

----------------------------------------
5.3 指纹验证流程
----------------------------------------

客户端启动时：
    1. 调用 get_machine_id() 获取本机指纹
    2. 发送 POST 到 /api/v1/validate，携带 machine_id
    3. 服务器检查白名单 + 有效期
    4. 返回 granted 或 denied

如果验证失败，弹窗显示本机指纹，方便用户复制给管理员

================================================================================
6. 服务器验证流程
================================================================================

----------------------------------------
6.1 启动时验证（server_client.py → validate_with_server）
----------------------------------------

    1. 获取机器指纹
    2. POST /api/v1/validate {"machine_id": "xxx", "current_date": "2026-05-28"}
    3. 如果返回 "granted" → 正常启动
    4. 如果返回 "denied" → 弹窗显示指纹 + 错误原因，退出程序
    5. 如果服务器不可达 → 弹窗显示指纹 + "服务器不可用"，退出程序

----------------------------------------
6.2 每天首次运行验证（automation_runner.py → run_script_main）
----------------------------------------

    1. 检查 _last_validated_date 是否等于今天
    2. 如果不是今天 → 重新调用 /api/v1/validate
    3. 验证通过 → 更新 _last_validated_date
    4. 验证失败 → 弹窗显示指纹，root.destroy() 退出程序

这确保了即使程序长时间不退出，每天也会重新验证一次

----------------------------------------
6.3 验证失败弹窗
----------------------------------------

所有验证失败的弹窗都包含本机机器指纹，格式：
    "机器指纹: a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6"
    "请联系管理员将此指纹添加到白名单"

================================================================================
7. Interception 驱动级键盘输入
================================================================================

本工具使用 Interception 驱动实现 WeGame 登录界面中账号密码的可靠输入，通过 ctypes 调用
interception.dll 实现驱动级键盘模拟（WeGame 支持多账号单独登录）。

----------------------------------------
7.1 模块结构
----------------------------------------

interception_keyboard.py（约 200 行）
    - 通过 ctypes 调用 interception.dll，使用驱动级键盘扫描码方式输入
    - is_available()：检查 DLL 加载 + context 创建（不发送测试按键）
    - send_string(text, interval)：逐字符发送按键（扫描码方式）
    - send_key(char, interval)：发送单个按键事件
    - _find_keyboard_device(ctx)：扫描设备 2-10，跳过只读 device 1，用
      interception_get_hardware_id() 检测真实硬件 ID 找到可写入的设备
    - _device_has_hardware_id(ctx, device)：检测设备是否有真实硬件 ID
    - 字符→扫描码映射表：支持字母、数字、常用符号、Shift 组合键
    - DLL 搜索路径：MEIPASS → exe 同级目录 → 项目目录 → System32 → LCA 安装目录
    - 发送时 set_filter=0（不拦截事件，只用于发送）

driver_keyboard.py
    - 键盘驱动统一入口
    - 所有操作委托给 interception_keyboard 模块

automation_runner._login_account()
    - WeGame 直接登录流程：
      1. 杀进程 → 打开 WeGame → 等待窗口出现
      2. 图像识别双击 account_select（左偏 15px）→ Ctrl+A + Backspace
      3. Interception 驱动级键盘输入账号
      4. 图像识别点击 Input（密码框）→ Interception 输入密码
      5. 图像识别点击 Sign-in 完成登录

----------------------------------------
7.2 驱动安装与状态
----------------------------------------

文件: interception.sys（内核驱动）
路径: C:\Windows\System32\drivers\keyboard.sys（伪装为系统键盘驱动）
服务: keyboard（内核驱动，手动启动）
DLL: interception.dll（项目根目录，打包时嵌入 exe 的根目录）

当前状态：
- 工作模式：正常模式（Windows 测试签名关闭，无需开启测试模式）

----------------------------------------
7.3 在新电脑部署驱动
----------------------------------------

方式一：使用安装程序（推荐）
    运行安装程序时勾选「安装 Interception 键盘驱动」即可自动安装。

方式二：使用 install_interception.bat
    以管理员身份运行安装目录下的 install_interception.bat，然后重启。

----------------------------------------
7.4 设备号说明
----------------------------------------

Interception 驱动中，设备 1 是只读中转设备（send 永远返回 0）。
真实键盘设备从 2 开始。程序自动扫描 device 2-10，通过硬件 ID 检测
找到可写入的键盘设备。

验证方式（管理员 CMD）：
    sc query keyboard      # 应显示 RUNNING

================================================================================
8. 心跳同步机制
================================================================================

文件: server_client.py（start_heartbeat / heartbeat_loop / send_heartbeat）

----------------------------------------
8.1 心跳线程
----------------------------------------

- 在任务开始时启动，任务结束时停止
- 每30秒发送一次心跳到服务器
- 使用 daemon 线程，不阻塞主程序退出

----------------------------------------
8.2 心跳数据格式
----------------------------------------

POST /api/v1/heartbeat
{
    "machine_id": "a1b2c3d4...",
    "accounts": [
        {"name": "qq1.png", "status": "running"},
        {"name": "qq2.png", "status": "cooling"},
        {"name": "qq3.png", "status": "success"}
    ],
    "is_running": true
}

----------------------------------------
8.3 账号状态值
----------------------------------------

- "running"  - 正在执行自动化任务
- "cooling"  - 跳过（冷却中）
- "success"  - 本账号执行成功
- "failed"   - 本账号执行失败
- "idle"     - 空闲/未执行

----------------------------------------
8.4 状态更新时机
----------------------------------------

在 automation_runner.run_script_main() 中：
    - 开始处理账号 → "running"
    - 冷却跳过 → "cooling"
    - 执行成功 → "success"
    - 执行失败 → "failed"
    - 任务全部完成 → 停止心跳线程

================================================================================
9. 如何管理用户（白名单/有效期）
================================================================================

----------------------------------------
9.1 服务器管理接口
----------------------------------------

所有管理接口需要 ADMIN_SECRET_KEY 认证（Header: Authorization）

1. 添加机器到白名单：
   POST /api/v1/admin/add_to_whitelist
   Body: {"machine_id": "a1b2c3d4..."}

2. 移除机器：
   POST /api/v1/admin/remove_from_whitelist
   Body: {"machine_id": "a1b2c3d4..."}

3. 设置独立有效期：
   POST /api/v1/admin/set_user_expiry
   Body: {"machine_id": "a1b2c3d4...", "expiry": "2026-12-31"}

4. 查看所有用户：
   GET /api/v1/admin/get_all_users

5. 查看机器实时状态：
   GET /api/v1/admin/get_machine_status

----------------------------------------
9.2 有效期管理逻辑
----------------------------------------

- 全局默认有效期：user_data.json 中的 "expiry_date" 字段
- 用户独立有效期：user_data.json → users → {machine_id} → expiry
- 优先级：用户独立有效期 > 全局默认有效期
- 修改有效期只需调用 set_user_expiry 接口，无需重新打包客户端

----------------------------------------

================================================================================
10. 打包说明（PyInstaller）
================================================================================

----------------------------------------
9.1 安装依赖
----------------------------------------

    pip install opencv-python numpy pyautogui psutil pywin32 pystray pillow pyinstaller rapidocr-onnxruntime

----------------------------------------
9.2 打包命令
----------------------------------------

    pyinstaller 三角洲自动工具.spec

----------------------------------------
9.3 打包配置（三角洲自动工具.spec）
----------------------------------------

关键配置:
    - hiddenimports: 包含所有依赖模块（含 6 个新子模块）
    - datas: 包含 picture 目录中的图片资源
    - 使用 collect_all 收集 rapidocr_onnxruntime 和 onnxruntime 的所有依赖
    - icon: 程序图标
    - uac_admin: 请求管理员权限

hiddenimports 包含的模块:
    config, utils, settings_window, template_capture,
    cooldown_manager, automation, machine_fingerprint, relative_mouse_move,
    email_notifier, account_manager, scheduler, cooldown_watcher,
    server_client, automation_runner, asset_db,
    psutil, win32gui, win32con, win32api, win32event, win32security,
    pystray, PIL, PIL.Image, cv2, numpy, pyautogui,
    smtplib, email, email.mime, email.mime.text, email.mime.multipart

----------------------------------------
9.4 添加新模块到打包
----------------------------------------

如果添加了新的 Python 模块，需要在 .spec 文件的 hiddenimports 列表中添加：

    hiddenimports=[..., '新模块名'],

----------------------------------------
9.5 输出
----------------------------------------

    dist/三角洲自动工具.exe    （单文件，约 100 MB）

================================================================================
11. 打包说明（Nuitka）
================================================================================

Nuitka 将 Python 编译为 C 代码后打包，体积更小、性能更好。

----------------------------------------
10.1 安装 Nuitka
----------------------------------------

    pip install nuitka

Nuitka 会自动下载 zig 编译器和 Dependency Walker。

----------------------------------------
10.2 打包模式
----------------------------------------

模式一：onefile（单文件，用于直接分发）
    python -m nuitka --standalone --onefile \
        --enable-plugin=tk-inter \
        --include-data-dir=picture=picture \
        --windows-icon-from-ico=picture/icon/icon.ico \
        --windows-uac-admin \
        --windows-console-mode=disable \
        --output-filename=三角洲自动工具.exe \
        --output-dir=dist_nuitka \
        --assume-yes-for-downloads \
        main.py

    输出: dist_nuitka/三角洲自动工具.exe （约 52 MB）

模式二：standalone（文件夹，用于安装程序分发）
    python -m nuitka --standalone \
        --enable-plugin=tk-inter \
        --include-data-dir=picture=picture \
        --windows-icon-from-ico=picture/icon/icon.ico \
        --windows-uac-admin \
        --windows-console-mode=disable \
        --output-filename=三角洲自动工具.exe \
        --output-dir=dist_nuitka_standalone \
        --assume-yes-for-downloads \
        main.py

    输出: dist_nuitka_standalone/main.dist/ （文件夹，约 195 MB）

----------------------------------------
10.3 Nuitka 与 PyInstaller 对比
----------------------------------------

    特性            PyInstaller       Nuitka
    --------------------------------------------------
    打包体积        约 95 MB          onefile 约 52 MB
    启动速度        较慢              较快
    运行性能        原生 Python       编译为 C，更快
    数据文件访问    sys._MEIPASS      __file__ 定位
    Python 版本     全版本支持        3.14 实验性支持
    编译时间        短                长（需 C 编译）

----------------------------------------
10.4 资源文件定位（resource_path）
----------------------------------------

config.py 中的 resource_path() 同时兼容两种打包方式：

    def resource_path(relative_path):
        if getattr(sys, 'frozen', False):
            # PyInstaller: sys._MEIPASS 指向临时解压目录
            base_path = getattr(sys, '_MEIPASS', ...)
        elif "__compiled__" in dir():
            # Nuitka: __file__ 指向编译后的模块位置
            # onefile 模式下指向临时解压目录
            # standalone 模式下指向 dist 文件夹
            base_path = os.path.dirname(os.path.abspath(__file__))
        else:
            # 开发环境
            base_path = os.path.abspath(".")
        return os.path.join(base_path, relative_path)

注意：Nuitka onefile 模式下，数据文件解压到系统临时目录，
__file__ 在编译模块中指向该临时目录，因此 resource_path 能正确定位。

----------------------------------------
10.5 Nuitka 注意事项
----------------------------------------

1. Python 3.14 仅被 Nuitka 4.x 实验性支持，建议使用 Python 3.13
2. --mingw64 不支持 Python 3.13+，Nuitka 会自动使用 zig 编译器
3. onefile 首次运行较慢（需解压），后续启动会利用缓存
4. standalone 模式更稳定，推荐用于安装程序分发

================================================================================
12. 制作安装程序（Inno Setup）
================================================================================

使用 Inno Setup 6 将打包后的程序制作成 Windows 安装程序。

----------------------------------------
11.1 安装 Inno Setup 6
----------------------------------------

下载地址: https://jrsoftware.org/isinfo.php
安装路径: C:\Program Files (x86)\Inno Setup 6

----------------------------------------
11.2 安装脚本（installer.iss）
----------------------------------------

项目根目录下的 installer.iss 是安装程序配置文件，关键配置：

    [Setup]
    AppId={固定GUID}              # 保持不变，用于覆盖安装识别
    AppName=三角洲行动自动化工具
    AppVersion=1.1.0              # 每次新版本更新
    DefaultDirName={autopf}\三角洲行动自动化工具
    SetupIconFile=picture\icon\icon.ico
    PrivilegesRequired=admin      # 需要管理员权限

    [Languages]
    # 使用简体中文语言包
    Name: "chinesesimplified"; MessagesFile: "compiler:Languages\ChineseSimplified.isl"

    [Files]
    # standalone 模式：将整个 main.dist 文件夹打包
    Source: "dist_nuitka_standalone\main.dist\*"; DestDir: "{app}"; \
        Flags: ignoreversion recursesubdirs createallsubdirs

    [Tasks]
    # 安装时可选：桌面快捷方式、开机自启动
    Name: "desktopicon"; Description: "创建桌面快捷方式"
    Name: "startupicon"; Description: "开机自动启动"

    [Run]
    # 安装完成后可选启动程序
    Filename: "{app}\三角洲自动工具.exe"; Description: "启动程序"; \
        Flags: nowait postinstall skipifsilent

----------------------------------------
11.3 编译安装程序
----------------------------------------

命令行编译:
    "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

或在 Inno Setup 编辑器中打开 installer.iss，点击 编译 → 执行。

输出: installer_output/三角洲行动自动化工具_安装程序.exe （约 49 MB）

----------------------------------------
11.4 覆盖安装
----------------------------------------

只要 AppId 保持不变，后续版本运行安装程序时会自动检测已有安装：
- 覆盖旧文件（ignoreversion 标志）
- 保留用户数据（配置文件存在 %USERPROFILE% 下）
- 更新开始菜单和桌面快捷方式

每次新版本只需：
1. 更新 installer.iss 中的 AppVersion
2. 重新打包 Nuitka standalone
3. 重新编译 Inno Setup 安装程序

----------------------------------------
11.5 语言配置
----------------------------------------

简体中文语言包位于:
    C:\Program Files (x86)\Inno Setup 6\Languages\ChineseSimplified.isl

在 installer.iss 的 [Languages] 段中引用：
    Name: "chinesesimplified"; MessagesFile: "compiler:Languages\ChineseSimplified.isl"

================================================================================
13. 更新日志
================================================================================

----------------------------------------
6.08.22 (2026-09-06b)
----------------------------------------

- 新增 AI 视觉验证 ai_visual_captcha.py（精简自 LCA-main 的「视觉 Agent」思路）：
  - 用途：WeGame 登录点击「登录」后可能弹出的**点击式图片验证码**（请依次点击/点选文字图标）
    自动处理；检测到滑块拼图验证时仅日志提示手动完成（不做拖动）
  - 流程：隐藏日志遮罩（防入镜）→ 彩色全屏截图（JPEG 90→55 逐级压缩 ≤4MB）→
    发国内视觉模型（OpenAI 兼容 /chat/completions，图走 base64 data URL，temperature=0）→
    解析严格 JSON {"captcha","type","targets":[{"text","bbox","point","scale"}]} →
    坐标归一化（scale=1000/1024 按比例、1 为 0-1 浮点、缺省像素；bbox 中心优先 point 兜底）→
    smooth_move_to + 拟人点击（间隔 0.6-1.2s 随机）→ 等 2.5s 重新截图复核，验证码消失即成功
  - 轮次：默认 5（可配 1-10）；「未检测到验证码」即返回成功；模型不给坐标宁可返回找不到（防幻觉）
  - 挂载点：_login_account 登录结果轮询的第三态（既未见重登按钮也未见三角洲图标）。
    启用且配置完整才触发（is_configured）；处理成功=登录成功；失败（滑块/无效回复/接口异常）=
    本轮登录失败走原有重试（最多 3 次）。未启用时行为与旧版完全一致
  - 供应商预设（下拉切换自动回填地址+模型，可完全自定义）：
    智谱GLM（glm-4v-flash 免费）/ 阿里百炼（qwen-vl-plus）/ 月之暗面Kimi（moonshot-v1-8k-vision-preview）/
    豆包火山方舟（doubao-1.5-vision-lite）/ 硅基流动（Qwen2.5-VL）/
    自定义；HTTP 用标准库 urllib（无新依赖）
  - 设置界面：全局设置页新增「AI视觉验证（登录验证码）」区块（启用勾选、供应商预设、
    API地址、API Key 密码态、模型、最大轮次、「测试识别」按钮——先落盘窗口配置再后台线程
    完整跑一遍检测处理，无验证码时不会点击任何东西）
  - 配置项：ai_visual_captcha_enabled（默认 False）/ ai_visual_captcha_provider /
    ai_visual_captcha_base_url / ai_visual_captcha_api_key / ai_visual_captcha_model /
    ai_visual_captcha_max_rounds（默认 5）
  - 打包：三角洲自动工具.spec hiddenimports 已加入 'ai_visual_captcha'
  - 测试：test_core_modules 新增 TestAiVisualCaptcha 8 项（JSON 提取容错、坐标归一化、
    none/slider/invalid 分支、预设表结构、is_configured），全部离线不联网不点击

----------------------------------------
6.08.22 (2026-09-06)
----------------------------------------

- 赛季公告 announcements.py 重构：
  - 状态改存独立文件 %APPDATA%\DeltaAutoTool\announcements.json（{"last_date","forever"}），
    不再写 settings.json —— 运行中多处会用「启动时的 settings 快照」整份覆盖写回
    （gui_app 托盘切遮罩/换角落、account_manager 自动备份），会把每日一次/永久关闭状态回滚；
    旧 settings 键（announcement_last_date / announcements_forever）首次读取自动迁移
  - 删除「一键配置」按钮，仅保留公告展示：按钮只剩「关闭」（今天不再提醒）与「永久不再提示」；
    点标题栏 X / Alt+F4 同样按「关闭」落盘（绑定 WM_DELETE_WINDOW，不再被绕过）
  - 正在跑自动化（app.running，含开机自启立即运行）时不弹，改为每 2 分钟重试检测、最多 60 次，
    修复自启场景自动化先于检测启动导致公告被永久静默跳过
  - 状态文件损坏时自动备份为 .corrupt.时间戳 后按空状态继续（可手动找回）
- 修复 relogin 重新登录后第二次 _launch_game 的结果漏判（automation_runner 两处）：
  原只判断 False/"relogin"，"game_failed" 落入成功分支（应记 24h 冷却+黄标）、"interrupted" 被忽略；
  现两次启动统一按 game_failed/interrupted 处理，二次启动仍要求重登按启动失败处理；
  重新登录失败路径用 launch_result=None 跳过后续判断，不重复记账
- template_insert_steps.py 可靠性：
  - _write_file 返回写盘成败；save 失败时不再更新内存缓存并返回 False
  - 编辑器（CustomOpsWindow 单列模式）保存失败弹窗提示，不再静默
  - 配置文件损坏时自动备份 .corrupt.时间戳 后按空配置继续，防止坏文件被下次 save 整份覆盖放大丢失
  - 旧 settings 迁移改 json 深拷贝（不再与 settings 缓存共享嵌套对象）
- cooldown_manager.py：
  - _restore_from_backup 增加候选结构校验 _plausible_cooldown_data（dict 且值均为 dict），
    跳过「非空但结构错误」的坏 .bak，让有效的时间戳备份可被轮询到
  - remove_expired_cooldowns / reduce_all_cooldowns：带 game_failed / auto_paused 标记的账号
    冷却到期时仅清冷却字段、保留标记（界面黄/橙标不随到期消失，与 reset_all_cooldowns 语义一致）
- custom_ops._run_batch_steps / _execute_step：
  - stop_event 贯穿单步内部：utils.find_and_click / ocr_find_and_click / find_image_on_screen
    新增可选 stop_event 参数（向后兼容），找图/OCR 超时窗口可被停止立即打断（重试会放大等待的问题）
  - 重试间隔与步骤停顿改用 Event.wait（停止可中断）；新增跳转次数上限 1000 补强死循环保护
  - 跳转/条件步骤配置了重试属性时提示「不支持重试，已忽略」；步骤字段为非法数值时宽容解析取默认值，
    不再抛 ValueError 炸出整批
- automation_runner.py：
  - 单账号结束后的后台冷却检查：期间保持 app.running=True，检查结束在主线程 on_finish
    （新增 _finish_after_cooldown_check），修复检查期间用户手动「开始」/冷却监听触发造成
    双流程同时驱动键鼠的竞态；start_single_account_run 重置 _cooldown_wait_done
  - _close_game：Alt+F4 兜底前重新激活游戏窗口（等待 WM_CLOSE 期间焦点可能漂移，
    防止 Alt+F4 发给其他窗口）；窗口激活逻辑提取为 _activate_delta_window
  - 登录结果轮询既未检测到重登按钮也未检测到三角洲图标（可能停在滑块验证/公告页）时，
    输出 OCR 屏幕文字诊断便于失败归因；轮询循环响应停止信号
  - _make_run_insert 回调异常改按失败处理（与 run_for_account 内部语义一致），不再吞成成功
- automation.py：邮箱领取入口：esc 后若找不到邮箱入口，补按一次 esc（关闭可能误开的系统菜单）
  再重试一次（自愈式，兼容「出售后停在仓库」与「未出售已在主界面」两种前置状态）
- utils.find_image_on_screen：模板缺失时节流打印告警（原静默返回 False，
  会导致登录成功/失败探测双双静默失效）
- 测试：公告测试改独立文件隔离，新增 旧 settings 迁移 / 损坏文件备份 用例；
  test_sell_items_meta 改临时目录隔离（不再读写真实 %APPDATA% sell_items 目录）

----------------------------------------
6.08.22 (2026-09-04)
----------------------------------------

- 键盘输入步骤：常用按键快捷行（空格/Tab/回车/Esc/方向键）仅在「方式=key」时显示，切到 text 自动隐藏
- 步骤编辑通用属性新增（自定义操作与模板插入步骤共用）：重试(次) op.retry、重试间隔(秒) op.retry_wait、失败处理可选（找不到图/字也继续下一步）op.optional —— custom_ops._run_batch_steps 失败后按 retry 重试、间隔 retry_wait，仍失败且 optional 则跳过继续不判失败
- 赛季公告 announcements.py：程序启动后若今天未弹过且未永久关闭则提醒一次
  - 存储 settings：announcement_last_date（最近提醒日期，同一天不重复）、announcements_forever（永久关闭的公告 id）
  - 按钮：「关闭」仅今天不再提醒（明天再弹）；「永久不再提示」写入 forever 永久不弹；「一键配置」给第10模板(特勤处入口 Special_Ops)写入点击前插入步骤 = 按空格 → OCR 找「开启新赛季」点击（该步 optional：平时无结算界面找不到自动继续第10步）；同时自动清理旧版误写到第9模板的段位结算配置；写入后当天不再提醒，并提示需手动重截第9模板图片
  - gui_app.main 启动后 3s 检测（app.running 时跳过，避免打断自动化）
- 点击日志改为「中文步骤名（坐标）」（如「启动游戏按钮（123，123）」，按 TEMPLATE_CAPTURE_LIST 由图片名映射，utils._find_and_click_core 成功点击与超时都显示中文名）
- 领取奖励空格由 1 次改 2 次：设施制造后领奖励（模板第20步）与邮箱/交易中心领取完成后（模板第27步）均按两下空格
- 邮箱货币领取结束收尾：由两次 esc（退出交易中心→退出邮箱）改为一次 esc 回主界面
- 登录结果轮询改为只探测不点击（新增 utils.find_image_on_screen）：原轮询用 find_and_click_smart 探测三角洲图标/重新登录按钮会真实点击，导致三角洲图标被点 2 次（探测一次 + _launch_game 一次），现改纯识别判断
- 修复「插入步骤跑完一次后消失」：插入步骤配置改存独立文件 %APPDATA%\DeltaAutoTool\template_insert_steps.json（不再放 settings.json）——运行过程中多处会用"启动时的 settings 快照"整份覆盖写回 settings.json，会把中途新配的插入步骤清掉；独立文件不受影响。旧版写在 settings.json 的配置在首次读取时自动迁移

----------------------------------------
6.08.22 (2026-09-02)
----------------------------------------

- 新增「模板插入步骤」功能：给任一模板（按 var_name，如 Hazard_Operations 烽火地带入口）配置一段自定义步骤，在自动化"识别点击该模板"的点击前 / 点击后执行
  - 数据存储：settings.json["template_insert_steps"][var_name] = {"timing":"before"|"after","steps":[custom_ops步骤]}；空 steps 即删除
  - 执行引擎复用 custom_ops._run_batch_steps（支持 jump/condition；上下文仅需 .settings，用 SimpleNamespace 兜底）
  - 入口：模板上传向导 → 模板设置窗口（恢复默认 与 测试 之间新增「插入步骤」按钮）→ CustomOpsWindow 单列模式（无工作流/频率，顶部「点击前/点击后」下拉）
  - CustomOpsWindow 单列模式：self.batches 合成单批、保存经 save_handler 回调不写全局 ops.json、_on_close 不 nav_pop 直接销毁并恢复父窗 grab
  - 红勾：向导每行状态区对配置了插入步骤的模板显示红色 ✔（默认行状态标签由 ttk.Label 改为 tk.Label 以支持染色）
  - 执行挂载点：automation_runner 登录（ACCOUNT_SELECT/密码输入框/SIGN_IN，before 在每次尝试点击前、after 在成功点击后，失败=该次登录尝试失败继续重试）、_launch_game（游戏图标/启动按钮/确定 可选）、手动单账号内联、game_operations（烽火地带/特勤处为门禁失败返 game_failed→1天冷却；Observe/Auto_fill 可选失败跳过；邮箱三连/一键出售失败结束该段账号继续）
  - automation.py 不 import 新模块：game_operations/handle_facility/sell_operations 增 run_insert 回调参数（runner 用 _make_run_insert(app) 注入），automation 内 _hook/_click 助手统一处理 before/after 与失败
  - 修复：settings 浅拷贝下直接改嵌套 dict 会污染 DEFAULT_SETTINGS（save 前 dict() 拷贝）

----------------------------------------
6.08.22 (2026-09-01)
----------------------------------------

- 日志遮罩回退调整：
  - 字体恢复 12px（撤销上一版 10px 紧凑化），内边距恢复
  - 实时鼠标坐标并入第一行行尾（状态左侧、坐标右侧同排显示，不再独占一行）
  - 移除找图/OCR 截图时自动隐藏遮罩（恢复常显；custom_ops 框选/点击的手动隐藏保留）
- 邮件表格增强（build_accounts_html）：
  - 已处理账号表格新增「资产」列（位于状态与下次运行之间），资产格式与主页一致（统一 M 格式）
  - 「下次运行」列去掉年份和秒，显示 08-31 15:00
- 组间等待修复与提示增强：
  - _group_wait 改用动态统计当前位置之后的就绪账号（含本次运行中新就绪的），修复用开跑前快照导致组间等待漏触发
  - 组间等待期间：主页操作栏 + 日志遮罩顶行均显示「组间等待 X分XX秒」实时倒计时（遮罩顶行临时覆盖账号状态，等待结束恢复）
- 测试隔离修复：cooldown 测试同时覆盖 COOLDOWN_JSON_BACKUP 并重置模块缓存，防止测试写入污染真实 cooldown.json.bak

----------------------------------------
6.08.22 (2026-08-30)
----------------------------------------

- DPI 设置修正：main.py 模块顶部新增 _set_dpi_awareness()，以 SetProcessDpiAwarenessContext(c_void_p(-4)，即 PER_MONITOR_AWARE_V2) 设置 DPI 感知，移除 config.py 中旧版 SetProcessDPIAware（与 Qt 高 DPI 冲突导致「拒绝访问」警告）
- WinError 740 修复：启动 WeGame 时若子进程要求提升权限（ERROR_ELEVATION_REQUIRED），改用 ShellExecuteW "runas" 提权启动，不再直接报错退出
- 日志遮罩线程安全：worker 线程不再直接调用 Qt 控件/processEvents，统一经 _app_root.after(0, ...) 转发到主线程执行（修复「运行测试/右键测试」卡死）
- 日志遮罩优化：
  - 内容紧凑化：字体 12px→10px、内边距缩小、行距 0，减少遮挡
  - 识别时自动隐藏：找图/OCR 截图前自动隐藏遮罩（避免遮挡按键/干扰截图匹配），识别完恢复（utils 增加 set_overlay_hooks，gui_app 注册 hide/show）
  - 组间等待倒计时：开启分组运行等待期间，主页操作栏显示「组间等待 X分XX秒」实时倒计时
- 分组运行勾选即时反馈：勾选「分组运行」后若冷却检测等待<10 自动改为 15（设置窗口输入框即时变化，无需等保存）
- 修复冷却等待漏跑已就绪账号：_wait_and_run_nearby_cooldowns 的 nearby 过滤原来只取 0<remaining<=窗口（把已冷却完成 remaining<=0 的账号排除），导致等待冷却中账号时忽略已就绪账号、且等待期间新到期的账号漏跑；现改为 remaining<=窗口（已就绪账号立即执行，wait 钳到0不等待），再等待冷却中的账号
- 分组运行组间等待改用动态统计：_group_wait 原用主流程开跑前的 runnable_total 快照算剩余就绪数，账号在组间等待期间冷却到期（新就绪）时快照失真、组间等待可能漏触发；改为动态统计当前位置之后的就绪账号（含本次运行中新就绪的）

----------------------------------------
6.08.22 (2026-08-28)
----------------------------------------

- 滑块验证功能移除：主程序删除滑块验证模块/配置/设置界面/登录接入/spec 引用，完整代码与重新接入说明移入「自动滑块验证/集成代码/」（slider_captcha.py + README.md）
- 账号运行分组（原「账号运行智能调度」改名）：
  - 设置 → 自动任务新增「账号运行分组」：启用开关、每组账号数（默认3）、组间隔分钟（默认5，含建议提示）
  - run_script_main 分组逻辑：每跑完 N 个实际处理的账号，若还有后续账号则组间等待指定分钟（set_operation + 可中断等待，等待期间主页操作栏显示组间等待倒计时），避免频繁切换账号触发滑块验证
- 账号数据备份定期自动清理：
  - 新增配置 account_backup_retention_days（默认3天，0=不清理，独立于日志保留天数）
  - cleanup_old_data 拆分：日志/截图日期文件夹按 log_retention_days 清理；「账号数据备份/」按 account_backup_retention_days 清理
  - 设置 → 其他设置 → 账号数据导入导出 新增「备份保留」设置项
- 分组运行细化（防关机漏跑）：
  - 组间等待逻辑：每跑满一组，只要还有就绪账号就等待（最后一批不超过一组，避免跳过等待导致连续跑太多触发验证）；预计算本轮就绪账号数，避免为全冷却的尾巴白等
  - 新增 cooldown_wait_minutes（冷却检测等待窗口，默认10分钟）：主流程后等待 N 分钟内到期的账号再跑；开启分组运行且设置<10 时自动为 15（避免组间隔+冷却差超过窗口导致提前关机漏跑）
  - 设置 → 自动任务 → 冷却执行 新增「冷却检测等待(分钟)」（从分组运行区移入）
- 修复邮箱货币领取后未返回主界面：领取完成后仅按一次 esc（退出交易中心回到邮箱界面），未退出邮箱回到主界面，导致二次资产识别/自定义操作找不到主界面目标；现退出交易中心后再按 esc 回到主界面；另修复一键出售后（未开邮箱时）停在仓库界面未回主界面

----------------------------------------
6.08.22 (2026-08-26)
----------------------------------------

- 观察状态功能：
  - 账号信息设置窗口「显示密码」左侧新增「观察状态」开关（点击进入/退出观察状态，点击立即写入并保存，无需按保存按钮）
  - 观察账号主界面「名称/备注」列最前面显示 ⚠ 标记
  - 主流程/单账号运行在按下烽火地带前多执行一步「观察状态入口」（可选模板，未上传或识别失败则跳过，不影响后续流程）
  - 模板上传向导新增可选模板「观察状态入口」（蓝色「非必需」，归入游戏内导航分组；模板 30→31）
  - 单账号运行识别烽火地带由 5 次降为 3 次（game_operations 新增 hazard_retry 参数化；主流程仍 5 次，观察账号主流程仍 5 次）
- 邮件内容修改（email_notifier / build_accounts_html）：
  - 邮件标题加入机器名称（电脑名，格式「xxx—运行报告」），账号失败通知/运行失败通知同样带机器名
  - 已处理账号前加备注（账号信息中的「备注」字段）用于分辨账号
  - 删除「基本信息」和「一键出售」板块
  - 已处理账号改为表格形式输出（账号｜状态｜下次运行，隔行底色）
- 一键出售鼠标移动：上架后不再固定移动到左上角 (20,20)，改为随机移动到距当前位置 300px 以上（utils.human_move_away）
- 界面调整：主界面「添加账号」靠左、「资产监测」靠右，「全体延时+/冷却缩减-」等分中间剩下的宽度；「上传模板图片」按钮左对齐；模板上传向导「模板设置」按钮向左移动（累计 60px）
- 托盘恢复：递归查找当前打开的子窗口（含从设置窗口嵌套打开的模板向导），点击托盘「显示窗口」恢复子窗口而非主窗口
- 删除文本：日志/备份保留提示去掉「默认3」；模板向导第 7 步（游戏确认按钮）提示去掉「在游戏启动后」、第 8 步（观察状态入口）去掉「在游戏主菜单，可选模板」
- 删除弃用代码：移除已弃用的 OCR 默认文本 GLOBAL_TEXT_DEFAULTS 及模板设置中的 _setup_ocr 死调用

----------------------------------------
6.08.22 (2026-08-21)
----------------------------------------

- 资产识别增强（防误识别）：
  - 进入特勤处前（大厅，烽火地带+空格空格Tab后）新增一次资产识别（候选A），与操作完成后的识别（候选B）对比校验
  - 校验规则（_validate_asset_pair）：两者都成功差≤3m且与最近记录≤50m（无记录无50m限制）取后一次；只成功一个则与最近记录≤50m有效；否则不存储
  - 大厅候选值每账号重置（app._asset_hub_value）
- 设施执行顺序随机：game_operations 中 4 个设施组（技术中心/工作台/防具台/制药台）random.shuffle 后再执行
- 设置界面说明文本缩短至≤30字（日志保留/定时任务兜底/驱动失败/自定义操作/自动备份间隔/售卖时间）；其他设置底部说明改为跟随窗口缩放换行
- 点击拟人抖动默认最大偏移 5 → 10（click_jitter_max）
- 模板第 20 步「领取奖励」（Claim_Reward）与第 27 步「领取完成确认」（EMAIL_RECEIVE_COMPLETED）改为直接按空格领取，不再图片识别点击；并从模板上传列表/OCR配置/常量中移除（模板 32→30）
- 开机自启动（--auto-start）静默运行：主窗口自动隐藏驻留托盘，不显示（可从托盘恢复）；run_on_startup 开启仍静默自动执行任务
- 版本号改用日期：6.08.22

----------------------------------------
v1.3.6 (2026-08-08)
----------------------------------------

- 冷却操作：
  - 主界面「全体延时」改为「全体延时+」：一键为所有冷却中账号延长 30 分钟（extend_all_cooldowns(hours=0.5)）
  - 主界面「冷却重置」改为「冷却缩减-」：删除原"重置全部"功能，改为给所有冷却中账号缩减 30 分钟（新增 reduce_all_cooldowns）
  - 缩减后剩余时间 ≤0 的账号视为冷却结束，直接移除冷却记录（变为可立即运行），不影响暂停账号
- 冷却持久化（崩溃保险）：
  - 保持"单个账号结束立即写盘"逻辑不变（_process_account_result → record_run → _save_data 原子写）
  - 新增 cooldown_manager.flush()：强制把内存冷却数据写盘并同步备份
  - on_finish 任务结束时调用 flush() 兜底保存，进一步降低异常退出/重启丢失冷却的风险
- 日志遮罩：
  - 默认开启（config.DEFAULT_SETTINGS enable_log_overlay: True），字体加粗（font-weight: bold）
  - 开启时在遮罩内打印一次关闭提示（托盘菜单「日志遮罩」或「实验功能」窗口）
  - 修复拖拽回弹：Qt 事件由连续 50ms 泵送改为事件驱动（有日志才 processEvents，空闲不泵送），
    避免遮罩 UpdateLayeredWindow 重绘干扰 Tk 主窗口拖拽导致窗口回到原位
  - 修复遮罩在账号切换时消失：根因是 _close_game 连发 3 次 alt+F4，游戏关闭后置顶遮罩成为前台窗口被下一次 alt+F4 误关
    - ScreenLogOverlay.closeEvent 免疫系统关闭事件（alt+F4 / WM_CLOSE），仅 disable_log_overlay 程序主动关闭时放行
    - 兜底：1 秒可见性看门狗（_start_qt_watchdog / _qt_watchdog_tick），遮罩被隐藏/销毁时自动恢复
      （仅在遮罩不可见时动作，拖拽时遮罩可见 → 不干扰，不影响拖拽修复）
- 窗口记忆：
  - 主窗口关闭后只记忆窗口大小，位置始终屏幕居中（不再恢复上次位置）
- 自定义操作：
  - 新增 custom_ops.py / custom_ops_window.py 模块：用户配置自定义操作步骤序列
  - 支持十种步骤类型（op.type）：
    - image 找图点击（默认）：编辑框内可框选/导入图片，匹配后点击（图片存 custom_ops/images/）
    - coordinate 坐标点击：固定坐标点击（x/y，支持「屏幕取点」），抖动时圆内随机偏移
    - ocr OCR点击：识别指定文字后点击（utils.ocr_find_and_click）
    - keyboard 键盘输入：按键/组合键（esc/Enter/ctrl+a 等）或文本（ASCII 用 pyautogui 输入，中文等非 ASCII 用剪贴板 + Ctrl+V；游戏内一律用 SendInput，Interception 驱动仅用于登录输入账号密码）
    - multi_image 多图点击：按顺序试多张图，点第一张找到的
    - drag 鼠标拖拽：起点→终点（smooth_move_to + mouseDown/Up）
    - scroll 鼠标滚轮：pyautogui.scroll（正上负下）
    - screenshot 截图保存：pyautogui 截图存到 全局设置→截图保存目录/日期/图片/账号名_时间.png（如 8月11日/图片）
    - condition 条件跳转：找图/OCR 探测（_probe_image/_probe_ocr），满足则跳到指定步骤
    - jump 无条件跳转：配合 condition 实现循环/重试
  - 运行时：while 循环 + 可变索引支持跳转；死循环保护（10000 次上限）
  - 步骤按「份」（批次）组织：ops.json = [{"name","max_runs","freq_days","steps":[...]}, ...]，旧格式自动迁移成单份
  - 频率限制按份独立：每份 max_runs/freq_days，run_key=账号::份名，should_skip_by_frequency / record_account_run；窗口按自然日计算（如 1次/1天=同一自然日最多1次），仅成功完成的工作流 record_account_run；
    主流程后所有份都执行，超限的份跳过、其他份照常；某步失败中止该份
  - 入口在「设置 → 自动任务」页最下方「配置自定义操作」；工具栏「＋ 添加步骤」菜单添加新类型，编辑框类型下拉中文
  - 份管理：份下拉切换、新增/删除份、份设置（名称 + 频率限制）
  - 设置窗口（底部「设置」按钮）：主流程后自动执行 / 随机偏移（截图保存目录在「全局设置」页）
  - 窗口布局：左下角运行测试/停止，右下角设置+保存步骤；右键/双击步骤可修改属性、运行本步骤、排序、删除
  - 步骤默认命名「类型中文名+序号」（坐标点击1/2）；主窗口与编辑框均记忆大小位置
  - 屏幕取点/框选：临时隐藏日志遮罩 + 释放模态 grab + 遮罩 grab 强制接收点击
  - 数据保存到 %APPDATA%\DeltaAutoTool\custom_ops\（ops.json + images/ + runs.json）
  - 配置项：enable_custom_ops / custom_ops_confidence / custom_ops_timeout / custom_ops_pause / custom_ops_jitter / custom_ops_max_runs / custom_ops_freq_days
- 设置窗口整理：
  - 点击随机偏移从实验功能移到「全局设置」页（资产识别下方、实用工具上方）
  - 账号数据导入导出从实验功能移到「其他设置」页最下方；更新底部说明（移除已无 UI 的鼠标下移/滚动幅度条目）
  - 实验功能窗口仅保留：驱动键盘测试、日志遮罩
  - 「皮肤抢购」按钮从「实用工具」移入「实验功能」窗口（新增皮肤抢购区块）；「实用工具」区域仅保留「实验功能」按钮，说明改为"驱动键盘测试、日志遮罩、皮肤抢购"
  - 删除「日志/截图保存目录」下方的说明文本（截图保存步骤说明不再显示）
  - 「额外等待时间」输入框取消右对齐，改为紧跟标签文本并在输入框后加「秒」单位；「其他设置」底部说明移除已删除的「账号间隔时间」条目
- 邮件通知：
  - 运行报告邮件与失败邮件新增「最先运行」和「最后运行」时间字段
  - 删除「冷却结束后发送邮件提醒」选项及 cooldown_email_enabled 配置；移除 email_notifier.send_cooldown_ready_email、cooldown_watcher 中冷却到期邮件发送逻辑、gui_app._send_cooldown_ready_email
  - 「邮件通知」页使用提示文本改为跟随窗口宽度自动换行（绑定 <Configure> 动态更新 wraplength）
- 冷却设置简化：
  - 删除「启用账号冷却」开关，账号冷却始终开启（config.DEFAULT_SETTINGS enable_cooldown=True，load_settings 强制 enable_cooldown=True 兼容旧配置）
  - 删除「账号间隔时间」设置（cooldown_delay_minutes）及 automation_runner._wait_account_interval 账号间隔等待逻辑
  - 「冷却小时数」保留，输入框后加「小时」单位
- 自定义冷却时间交互调整：
  - 双击主界面账号列表「下次运行时间」列打开自定义冷却时间（account_manager.double_click_next_run，仅 next_run 列生效），替代原右键菜单「自定义冷却时间」
  - 输入改为「两个数字框 + 固定 H/M 单位文本」（如 8H 30M），默认预填 8H 0M，小时框自动聚焦全选；回车或点确认生效
  - 删除「取消」按钮，仅保留「确认」（可点窗口右上角 × 关闭）
- 一键出售：
  - sell_operations 将「未配置售卖物品」检查提前到函数开头：未添加任何售卖物品时完全跳过出售流程（不进入仓库），日志提示"未配置任何售卖物品，跳过售卖"
- 冷却数据可靠性修复：
  - 修复暂停账号冷却被误清：原 remove_expired_cooldowns 对暂停账号无条件清除冷却字段（每 30 秒冷却监听触发一次），导致暂停中的账号冷却进度丢失
  - 现改为：暂停账号暂停期间保留冷却进度（不清时间戳），仅在冷却真正到期时清除冷却字段、保留 account_paused 标记；非暂停账号逻辑不变
  - 冷却保存新增带时间戳的历史备份（_create_timestamped_backup）：每次 _save_data 覆盖前复制当前 cooldown.json 为 cooldown.json.bak.时间戳，
    限频 30 秒 + 轮转保留最近 30 份，逻辑性数据清空/误操作时可通过历史备份找回
  - 冷却损坏保护：主文件存在但解析失败且无可用备份时标记 _load_corrupt，_save_data 拒绝用空/残缺数据覆盖磁盘（防止崩溃后冷却与暂停标记被一起抹掉）
  - 恢复兜底扩展：_restore_from_backup 候选顺序 .bak → 最近的时间戳备份，主文件与 .bak 同时损坏时也能从历史快照找回（含暂停标记）
- 自定义操作键盘步骤修复：
  - text 模式支持中文等非 ASCII 文本：中文走剪贴板 + Ctrl+V 粘贴（win32clipboard），ASCII 用 pyautogui 输入
  - 约束：Interception 驱动仅用于 WeGame 登录时输入账号密码；游戏内（自定义操作键盘步骤）一律使用 pyautogui（SendInput），不再调用 Interception 驱动，避免驱动在游戏场景被误用
- 游戏关闭方式修复（蓝屏 0x139/0xBE）：
  - 用户反馈：对全屏游戏直接注入 Alt+F4 强制退出时，偶发蓝屏（KERNEL_SECURITY_CHECK_FAILURE / ATTEMPTED_WRITE_TO_READONLY_MEMORY），手动关闭游戏几乎不触发
  - _close_game 改为「优雅关闭优先」：先发送 WM_CLOSE 并等待进程退出（最多约 8 秒），仅当无效时再兜底 Alt+F4（SendInput）+ 强制结束
  - 新增 _game_process_running() / _wait_game_process_exit() 辅助函数；主循环在中断时跳过 _close_game 的逻辑保持不变
- 日志/截图目录整理：
  - 日志与截图共用 log_save_path 根目录，日期文件夹内再分「日志」「图片」子文件夹：如 8月11日/日志/、8月11日/图片/
  - 新增 utils.date_folder_name()：返回「M月D日」（如 8月11日）；gui_app 日志路径与 custom_ops 截图路径统一使用
- 实验功能新增「测试关闭游戏」：
  - 设置 → 实验功能 → 测试关闭游戏区块，点击「关闭三角洲游戏」后台调用 automation_runner._close_game
  - 用于手动验证优雅关闭（WM_CLOSE 优先）是否生效、是否仍触发蓝屏
- 账号数据自动备份（防崩溃/蓝屏导致账号数据丢失）：
  - 设置 → 其他设置 → 账号数据导入导出 新增「自动备份间隔」选项（0-3 天，0=关闭，默认 0）
  - account_manager.auto_backup_account_data()：将 accounts.json + cooldown.json + settings.json 复制到「目录及数据/账号数据备份/」，
    文件名带时间戳（如 accounts.json.20260812_0217.bak）；备份与运行数据完全隔离，崩溃/蓝屏不影响
  - 触发时机：程序启动时（gui_app._continue_init）+ 每次任务完成后（automation_runner.on_finish），按 account_backup_last 检查间隔
  - 配置项：account_backup_days / account_backup_last
- 「日志/截图保存目录」改名「目录及数据」：
  - 设置 → 全局设置 的目录项改名为「目录及数据」（现包含日志、截图、账号数据备份）；log_save_path 含义不变
- 自定义操作频率窗口修复：
  - should_skip_by_frequency 由「滚动 24h×N 窗口」改为「按自然日计算」：1次/1天 = 同一自然日最多执行 1 次（今天跑过今天就跳过，与上次运行是否满 24h 无关）
  - 计数仍仅针对成功完成的工作流（record_account_run 只在批次全部步骤成功时调用）
- 自动清理日志/备份：
  - account_manager.cleanup_old_data()：按 log_retention_days 保留天数（默认 3，0=不清理）清理「目录及数据」下过期的日期文件夹（如 8月11日/，含日志与图片）和「账号数据备份/」中过期的 .bak 文件
  - 触发时机：程序启动（gui_app._continue_init）+ 任务完成后（automation_runner.on_finish）
  - 配置项：log_retention_days；设置→全局设置→目录及数据 可修改保留天数（0-90）
- 连续失败自动暂停修复：
  - 修复原「连续失败 2 次自动暂停」失效问题：计数改为跨轮累计（不再每轮重置，成功或手动恢复才清零），达到 2 次真正调用 set_account_paused 暂停账号
  - 自动暂停的账号在列表标黄（auto_paused 标记 + 新 tag #f1c40f），手动暂停仍为红色；手动暂停/恢复时同步清除 auto_paused 标记
  - 不弹窗提示，仅日志输出「账号 xxx 连续失败 N 次，自动暂停」
- 四项体验优化：
  - 资产记录窗口「清除记录资产」按钮改为先 pack 到右侧，小窗口下不再被统计标签挤掉而看不见
  - 右键单账号运行：进入烽火地带与主流程一致增加最多 5 次重试（未找到图标 5 秒后重试），修复单账号无重试问题
  - 日志去重：新增 utils.throttled_print，高频重复消息（如反复「未找到包含 xx 的窗口」）同一消息 3 秒内只打印一次，减少刷屏
  - 日志遮罩：新增固定顶行「正在运行第x个账号:'账号';运行时长:'xHxMxS'」（空闲显示「未运行」，运行中每秒刷新）；实验功能「启用日志遮罩」旁新增「更换角落」按钮，逆时针旋转 左下→右下→右上→左上→左下，位置持久化（log_overlay_corner），关闭重开恢复
- 账号列表交互调整：
  - 双击按列分派（account_manager.double_click_column）：双击「现有资产」列→资产记录窗口；双击「下次运行时间」列→自定义冷却；双击「名称/备注」列→账号信息设置；QQ账号列无动作
  - 右键菜单移除「查看资产记录」「账号信息设置」（改由双击进入），保留 运行此账号/上移下移/重置冷却/暂停/删除
  - 底部提示改为轮换播放：9 条操作提示，每条前缀「提示：」总长≤30 字，每 60 秒（账号列表周期性刷新）切换下一条（gui_app._HINT_TIPS + _rotate_hint）

----------------------------------------
v1.3.5 (2026-08-01)
----------------------------------------

- 日志优化：
  - 日志按日期文件夹分文件存储：日志保存路径/YYYY-MM-DD/HH-MM-SS.log
  - 每次运行开始自动创建独立日志文件，首次启动为 startup.log
  - RedirectText 新增 set_log_path() 动态切换日志文件
  - App 新增 _set_run_log_file() 方法，按日期+时间命名日志
- 错误诊断：
  - 新增 _ocr_capture_screen_text()：账号失败时自动 OCR 识别屏幕文字输出到日志
  - 日志格式：📋 屏幕文字: ... （后续可根据关键词做错误分类和邮件通知）
- 开发者测试窗口精简：
  - 删除账号登录测试、进程清理测试、窗口查找测试、图像识别测试
  - 删除文本识别测试、图片识别置信度测试
  - 仅保留驱动键盘测试；修复窗口关闭后记忆大小功能
- 模板设置集成置信度测试：
  - 模板设置窗口新增「测试」按钮（居中），点击弹窗显示该模板匹配置信度
  - 新增 _test_template_confidence()：截图前隐藏窗口避免遮挡，匹配后弹窗显示结果
- 运行优化：
  - start_run() 启动时自动取消待执行关机计划（冷却触发新任务时不再误关机）
- 输入保护：
  - Interception 输入账号密码前自动检测并关闭 Caps Lock（_is_capslock_on + _send_toggle_key）
  - 防止大写锁定导致账号密码输入错误
- 冷却数据备份：
  - cooldown.json 保存前自动备份为 .bak（COOLDOWN_JSON_BACKUP）
  - 主文件不存在/损坏/为空时自动从备份恢复（_restore_from_backup）
- 全体延时：
  - 主界面「清空列表」按钮改为「全体延时」（extend_all_cooldowns）
  - 一键为所有冷却中账号延长 1 小时冷却，不影响暂停账号
- 账号数据导入导出：
  - 开发者测试窗口新增「账号数据导入导出」区域（_export_accounts / _import_accounts）
  - 导出：保存账号列表/密码备注/资产及历史到用户选择文件
  - 导入：从文件加载覆盖当前数据，导入前自动备份 + 校验格式
- 启动性能优化（解决验证卡顿/未响应）：
  - get_machine_id() 移到后台验证线程，UI 线程不再触碰 WMI
  - 硬盘/主板序列号成功结果缓存（_disk_serial_cache / _board_serial_cache），一次查询全程序复用
  - wmic/PowerShell 查询超时从 10s 降到 3s
  - 删除死代码 _show_machine_fingerprint（机器指纹显示在设置中已异步加载）
- 点击拟人抖动：
  - 游戏内操作（设施/邮箱/出售）点击位置随机偏移（圆内随机 ≤5px），拟人化减少机械化痕迹
  - 新增 set_click_jitter() + _find_and_click_core 内嵌偏移逻辑
  - 开发者测试窗口新增「点击随机偏移」开关（可调 0-20px）
  - 登录/启动阶段不加偏移，仅游戏内操作（game_operations_wrapper / sell_operations_wrapper）
- 修复桌面图标消失：
  - 删除两处「关闭前台窗口」危险代码（GetForegroundWindow + WM_CLOSE）
  - 原逻辑会误关桌面 Explorer（焦点在桌面时导致图标消失、右键失效）
  - 进程清理由 _cleanup_processes 负责，不再需要关闭前台窗口
- 自动缩到托盘：
  - 运行自动化时主窗口+日志窗口自动隐藏到系统托盘（_hide_to_tray），防止遮挡游戏
  - 运行完成自动恢复前台（on_finish 调 _show_window）
  - 仅托盘可用时隐藏，避免窗口无法恢复
- 日志遮罩（PyQt6 透明叠加层）：
  - 屏幕左下角透明日志层：无边框/置顶/鼠标穿透，不干扰游戏操作
  - QPlainTextEdit 分级彩色日志（DEBUG灰/INFO浅蓝/WARN黄/ERROR红）+ 时间戳
  - 严格线程安全：pyqtSignal 信号槽，子线程仅 emit，主线程槽更新控件
  - 默认关闭、延迟加载、销毁释放；托盘菜单 + 实验功能窗口双开关
  - 交互模式可拖动窗口；已移除 F8/F9 全局热键（穿透模式收不到键盘事件）
  - RedirectText 转发日志到遮罩（按内容分级：❌失败→红，⚠️警告→黄，其他→蓝）
- 清理：
  - 删除未使用的三角洲路径（delta_path / DELTA_PATH，从未被程序使用）
  - 「开发者测试」改名「实验功能」（按钮、窗口标题同步更新）
  - 修复托盘菜单动态文本 lambda 缺 item 参数导致图标消失的 bug
- 打包：
  - 统一打包 PyQt6 全功能版（增量约 27MB），日志遮罩对所有用户可用
- 文档修正：
  - 内置模板基于 2560×1440（2K）屏幕 + 游戏 1440×900 制作

----------------------------------------
v1.3.4 (2026-07-12)
----------------------------------------

- 新增功能：
  - 皮肤抢购模块：OCR监控倒计时 + 余额检测 + 超时自动停止
  - 窗口导航栈管理：打开新窗口自动隐藏旧窗口，返回时自动恢复
  - 图片匹配上传：pHash 感知哈希匹配，支持单图/多图/文件夹顺序匹配
  - 模板向导按主流程顺序排序（登录→导航→设施→制造→邮箱→出售）
  - 新增 ENSURE 可选确认步骤（游戏启动后的「确定」按钮，蓝色非必需标记）
  - 每个账号运行前自动取消待执行关机计划
- Bug 修复：
  - 修复 _run_single_account_main 未设置 app._current_account_name，导致单账号资产识别不记录
  - 修复清除资产记录后重启旧数据重新出现（_clear_asset_records 未保存 accounts.json）
  - 修复资产识别结果重启后丢失（_recognize_and_store_asset 未保存 accounts.json）
  - 修复单账号运行重置已有冷却（改为保留剩余冷却时间）
  - 修复单账号运行触发延时关机和邮件提醒（改为跳过）
- 驱动修复：
  - Interception 安装：服务名改为 keyboard（与官方一致），新增 sc start 启动驱动
  - 安装程序新增释放 interception.dll 到程序目录
  - install_interception.bat 支持一键安装并启动驱动
- 代码清理：
  - 删除 migrate_cooldown_keys() 和 get_cooldown_key() 死代码
  - 删除旧版纯列表格式兼容、备注多 key 回退查找、向后兼容测试
  - 删除 account_manager.py 重复的 _get_cooldown_key 别名
- 日志优化：
  - kill_process 未找到进程时静默返回
  - record_run 定时任务兜底去重（仅最早冷却时间变化时更新）
  - cooldown_watcher 冷却移除日志去重
- UI 改进：
  - 模板向导进度文字改为 4/31 格式，打开即显示
  - 匹配窗口：居中显示、待上传文本、取消/保存与导航同行
  - 主界面「账号名称」改为「QQ账号」

v1.3.3 (2026-06-29)
----------------------------------------

- Bug 修复：
  - 修复 account_manager.py 未定义变量 bug（cd_name→name），导致每 60 秒崩溃
  - 修复 _match_template_multiscale 返回值不匹配（5 个值 vs 4 个值）
- 代码清理：
  - 删除 9 个死代码函数（get_sell_items、pause_cooldown、resume_cooldown、clear_game_failed、key_press、hold_key、ocr_find、ocr_find_and_click_offset、write_cooldown_signal）
  - 删除 driver_keyboard.py 中未使用的函数，精简为 3 个函数
  - 清理旧登录方式残留代码（QQ 降级方案测试、描述文字）
- 功能优化：
  - 账号间隔默认值改为 0
  - 资产识别流程优化：主流程两次识别（进入特勤处后 + 操作完成后）
  - 单账号运行增加进入特勤处和资产识别
  - 右键「运行此账号」不受冷却和暂停限制
  - 设置界面只显示 Interception 状态，移除 PyDirectInput 显示
- 测试更新：
  - 删除 QQ 降级方案相关测试（9 个）
  - 删除 pause_cooldown/resume_cooldown 相关测试
  - 测试数量从 52 个精简到 43 个

----------------------------------------
v1.3.2 (2026-06-27)
----------------------------------------

- 代码审查全面修复：
  - save_settings 原子写入防崩溃丢设置
  - cooldown_manager 线程安全锁修复
  - RedirectText 后台线程安全
  - _last_account_error 跨账号清除
  - _launch_game 区分中断和失败
  - delete_account 清理冷却数据
  - move_up/down 持久化排序
  - 编辑账号迁移冷却数据
  - 模板缓存和 OCR 失败跟踪加锁
  - 登录后验证 login_again
  - 关闭游戏前先激活窗口
  - 右键菜单标签动态查找

----------------------------------------
v1.3.1 (2026-06-27)
----------------------------------------

- 模块化重构：
  - get_cooldown_key 提取到 cooldown_manager.py 作为统一函数
  - _ensure_game_focused 统一到 automation.py
  - scheduler.py 用 find_earliest_cooldown 替代内联循环
- 新增功能：
  - 删除 PyDirectInput 依赖，统一使用 Interception 驱动
  - 设置界面新增「Interception 驱动失败时自动重启电脑」选项
  - 右键账号新增「运行此账号」功能
  - 游戏内操作失败自动设置 1 天冷却
  - 连续失败 2 次自动暂停账号
  - 冷却到期定时任务兜底机制
- 代码质量优化：
  - save_settings 原子写入
  - cooldown_manager 线程安全
  - OCR 引擎异步初始化
  - Treeview 列宽优化

----------------------------------------
v1.3.0 (2026-06-19)
----------------------------------------

- 移除凭据加密模块，settings 改为纯 JSON 明文读写
- 冷却线程安全锁
- 模板缓存 LRU
- OCR 自动恢复（3 次连续失败临时禁用）
- 降价逻辑优化（首次识别后原地点击）

----------------------------------------
v1.2.1 (2026-06-17)
----------------------------------------

- 资产记录双击编辑
- 失败邮件含错误日志
- WeGame 登录窗口聚焦保护
- 冷却到期优先运行 + 10 分钟等待
- 删除 QQ 无效设置
- 售卖物品元数据原子写入 + 备份保护

----------------------------------------
v1.2.0 (2026-06-14)
----------------------------------------

- 登录方式重构：WeGame 直接登录（Interception 驱动级键盘输入）
- 代码重构优化：合并 find_and_click、缓存 settings、消除冗余转换
- 截图对象内存泄漏修复
- 新增 interception_keyboard.py 模块

----------------------------------------
v1.1.5 (2026-06-13)
----------------------------------------

- QQ 账号密码登录 + WeGame 快捷安全登录
- 密码必填校验
- 主界面 Treeview 四列显示
- 账号序号、资产监测自定义天数

----------------------------------------
v1.1.4 (2026-06-10)
----------------------------------------

- UI 简化：删除电源管理页签和定时执行功能
- 线程安全：threading.Event 包装
- 冷却缓存优化
- 核心模块单元测试

----------------------------------------
v1.1.3 (2026-06-09)
----------------------------------------

- 暂停账号功能
- 批量添加账号
- 账号列表分隔线
- QQ 登录逻辑优化

----------------------------------------
v1.1.2 (2026-06-08)
----------------------------------------

- 冷却暂停/恢复功能
- 账号状态颜色可视化
- 鼠标贝塞尔曲线移动
- 邮件通知优化

----------------------------------------
v1.1.1 (2026-06-07)
----------------------------------------

- 资产监测功能（SQLite 持久化）
- 账号备注功能
- 资产记录窗口优化

----------------------------------------
v1.1.0 (2026-06-06)
----------------------------------------

- OCR 文字识别
- 资产识别
- 模块拆分（gui_app.py 拆分为 7 个文件）
- 设置窗口重新布局

----------------------------------------
v1.0.3 (2026-05-28)
----------------------------------------

- 机器指纹绑定
- 远程有效期控制
- 账号状态实时同步（心跳推送）
- 运行完成后延迟关机

----------------------------------------
v1.0.0 (2026-05-25)
----------------------------------------

- 初始发布版本

================================================================================
14. 服务器部署
================================================================================

1. 将 server.py 上传到云服务器
2. 安装依赖: pip install fastapi uvicorn
3. 启动: python server.py 或 uvicorn server:app --host 0.0.0.0 --port 8000
4. 管理后台: 将图形化可视管理后台代码部署为 HTML 页面

================================================================================
15. 常见问题
================================================================================

Q: 如何测试机器指纹验证？
A:
    1. 运行 python machine_fingerprint.py 查看本机指纹
    2. 确保该指纹在服务器白名单中
    3. 如果不在白名单，调用 /api/v1/admin/add_to_whitelist 添加

Q: 服务器不可达时程序会怎样？
A:
    程序会弹窗显示本机指纹和"服务器不可用"提示，然后退出。
    不会降级到本地验证，确保安全性。

Q: 用户说程序打不开怎么办？
A:
    1. 让用户截图弹窗中的机器指纹
    2. 在管理后台调用 add_to_whitelist 添加该指纹
    3. 如果是有效期问题，调用 set_user_expiry 延长有效期

Q: 如何查看哪些机器在线？
A:
    调用 GET /api/v1/admin/get_machine_status，返回所有机器的实时状态。
    超过5分钟无心跳的机器会被标记为离线。

Q: 心跳数据不更新了怎么办？
A:
    1. 检查客户端程序是否还在运行
    2. 检查服务器是否在线
    3. 心跳停止不影响本地自动化功能，只是无法远程监控

Q: 如何修改服务器地址？
A:
    修改 config.py 中 DEFAULT_SETTINGS 的 server_url 字段，重新打包。
    或者让用户在 ~/.delta_auto_settings.json 中手动修改。

Q: 打包后程序无法启动？
A:
    1. 检查所有新模块是否在 hiddenimports 中（PyInstaller）
    2. 检查图片资源是否正确打包
    3. 查看 dist 目录下的错误日志
    4. 确保目标机器能访问服务器

Q: Nuitka 打包后图片资源找不到？
A:
    1. 确认使用了 --include-data-dir=picture=picture 参数
    2. 确认 resource_path() 中 Nuitka 分支使用 __file__ 定位
    3. standalone 模式下确认 picture/ 目录在 exe 同级目录
    4. onefile 模式下数据解压到临时目录，重启后可能需重新解压

Q: Inno Setup 安装后程序无法运行？
A:
    1. 确认安装目录下有 picture/ 目录及所有子目录
    2. 确认以管理员权限运行（电源管理功能需要）
    3. 检查目标机器是否安装了 VC++ 运行库

Q: OCR 识别不工作怎么办？
A:
    1. 检查是否安装了 rapidocr-onnxruntime 依赖
    2. OCR 连续超时 2 次会自动禁用，重启程序可恢复
    3. 检查 OCR 配置是否正确（识别区域、目标文本）
    4. 如果 OCR 引擎初始化失败，程序会自动回退到图像匹配

Q: 资产识别不工作怎么办？
A:
    1. 在设置→全局设置中勾选"启用资产识别"
    2. 点击"设置区域"框选游戏中的资产数值区域（如 "1.2M"）
    3. 点击"测试识别"验证是否能正确识别
    4. 确保游戏画面稳定后再识别（程序会等待 1 秒）
    5. 资产识别使用独立的 RapidOCR 引擎实例（缓存在 automation_runner._ocr_engine）
    6. 注意：资产识别不做 BGR 转换，直接传 numpy 数组给 RapidOCR

================================================================================
                            文档结束
================================================================================

如有问题，请联系开发团队。

最后更新: 2026-06-24
